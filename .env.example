# Digital Twin Configuration
# No API key needed - uses Claude Code CLI (your existing subscription)

# Optional: Set a daily budget warning threshold (for tracking purposes)
# DAILY_BUDGET_WARNING=50
